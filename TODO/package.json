{
  "name": "todo-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test:e2e": "start-server-and-test 'cd ../todo_backend && npm start' http://localhost:5000/health start http://localhost:3000 cypress:run",
    "test:e2e:open": "start-server-and-test 'cd ../todo_backend && npm start' http://localhost:5000/health start http://localhost:3000 cypress:open"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^4.11.0",
    "react-scripts": "5.0.1",
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "cypress": "^13.3.0",
    "start-server-and-test": "^2.0.1"
  }
}